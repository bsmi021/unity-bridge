"""MCP server integration for Unity Bridge.

Provides backward-compatible MCP tool access using the same core
async functions as the CLI.  Start with ``unity-bridge serve`` or
``python -m unity_bridge.mcp.server``.
"""
